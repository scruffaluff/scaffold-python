{
  "scripts": {
    "format": "prettier --write **/*.{css,html,js,json,md,yaml,yml}",
    "test": "prettier --check **/*.{css,html,js,json,md,yaml,yml}"
  },
  "devDependencies": {
    "prettier": "^1.19.1"
  },
  "prettier": {
    "arrowParens": "always",
    "printWidth": 80,
    "proseWrap": "always",
    "semi": true,
    "tabWidth": 2,
    "trailingComma": "es5"
  }
}
