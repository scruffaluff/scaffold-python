{
  "private": true,
  "scripts": {
    "format": "prettier --write **/*.{css,html,js,json,md,ts,yaml,yml}",
    "test": "prettier --check **/*.{css,html,js,json,md,ts,yaml,yml}"
  },
  "devDependencies": {
    "prettier": "^2.0.5"
  },
  "prettier": {
    "arrowParens": "always",
    "printWidth": 80,
    "proseWrap": "always",
    "semi": true,
    "tabWidth": 2,
    "trailingComma": "es5"
  }
}
